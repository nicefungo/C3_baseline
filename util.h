
#include <cmath>
#include <vector>

/*
template <typename T>
T gelu_(T x) {
    const T kAlpha = static_cast<T>(0.7978845608028654); // sqrt(2.0 / PI)
    const T kBeta = static_cast<T>(0.044715);
    T x_cubed = x * x * x;
    return static_cast<T>(0.5f) * x * (1.0f + std::tanh(kAlpha * (x + kBeta * x_cubed)));
}
*/
